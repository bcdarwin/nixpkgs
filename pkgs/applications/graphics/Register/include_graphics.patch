diff --git a/CMakeLists.txt b/CMakeLists.txt
index 54ae46d..c82eb49 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -49,6 +49,7 @@ IF(NOT MINC_TOOLKIT_BUILD)
 
   FIND_PACKAGE( LIBMINC REQUIRED )
   FIND_PACKAGE( BICPL REQUIRED )
+  FIND_PACKAGE( bicgl REQUIRED NAMES BICGL)
 #  FIND_PACKAGE( Perl REQUIRED )
   FIND_PACKAGE( Threads )
   FIND_PACKAGE( OpenGL REQUIRED )
@@ -73,7 +74,7 @@ SET(PACKAGE_STRING "${PACKAGE_NAME} ${PACKAGE_VERSION}")
 INCLUDE( ${LIBMINC_USE_FILE} )
 INCLUDE( ${BICPL_USE_FILE} )
 
-SET(CMAKE_REQUIRED_INCLUDES ${CMAKE_REQUIRED_INCLUDES} ${LIBMINC_INCLUDE_DIRS} ${BICPL_INCLUDE_DIRS})
+SET(CMAKE_REQUIRED_INCLUDES ${CMAKE_REQUIRED_INCLUDES} ${LIBMINC_INCLUDE_DIRS} ${BICPL_INCLUDE_DIRS} ${BICGL_INCLUDE_DIRS})
 
 ADD_DEFINITIONS(-DHAVE_CONFIG_H)
 IF(NOT APPLE)
@@ -125,8 +126,8 @@ CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/epm-header.in  ${CMAKE_CURRENT_BINARY
 
 INCLUDE_DIRECTORIES( ${CMAKE_CURRENT_BINARY_DIR} ${CMAKE_CURRENT_SOURCE_DIR})
 INCLUDE_DIRECTORIES( ${GLUT_INCLUDE_DIR} )
-INCLUDE_DIRECTORIES( ${BICGL_INCLUDE_DIRS} )
-INCLUDE_DIRECTORIES( User_interface/Include Functionality/Include UI_calls_F/Include  )
+INCLUDE_DIRECTORIES( ${BICGL_INCLUDE_DIRS}/Include )
+INCLUDE_DIRECTORIES( User_interface/Include Functionality/Include UI_calls_F/Include )
 LINK_DIRECTORIES( ${BICGL_LIBRARY_DIRS} )
 
 
